# Healthy Web 官方前端构建不需要浏览器侧密钥。
# HEALTHY_WEB_ENABLED 与 HEALTHY_WEB_BINDING_READ_ENABLED 只配置在服务端运行环境。
HEALTHY_WEB_ENABLED=false
HEALTHY_WEB_BINDING_READ_ENABLED=false

# rockyBinding 微信云函数默认关闭，完成中央 Healthy grant 和 A/B 负向验证后再开启。
HEALTHY_ROCKY_BINDING_ENABLED=false
