{
  "name": "healthy-pro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5173 --bind 127.0.0.1",
    "dev:lan": "python3 -m http.server 5173 --bind 0.0.0.0",
    "build": "node scripts/check-app.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
